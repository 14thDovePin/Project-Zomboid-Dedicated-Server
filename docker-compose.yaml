services:
  dedicated-server:
    container_name: dedicated-server
    image: dedicated-server:latest
    build:
      network: host
      context: build
    environment:
      - sample_text="Hello World!!"
    volumes:
      - ./server-files:/home/steam/server-files
      - ./SERVER-data:/home/steam/.SERVER_DATA
    ports:
      - 25565:25565
    stdin_open: true
    tty: true